Docker support
..................

Docker support is added, but targets linux which doesn't support SQL Server LocalDB. 
Fill in a valid SQL Server Connectionstring in appsettings.json to get it to work, or use windows containers for Docker.